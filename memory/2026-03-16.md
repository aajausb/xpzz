# 2026-03-16 日记

## 00:00-00:30 dex_trader v2 重写 + 全链路测试

### dex_trader.js 完整重写（v2）
- 从430行精简到280行，修掉8个问题：
  1. SOL sell查余额用`getTokenAccountsByOwner`(raw buffer)→改`getParsedTokenAccountsByOwner`(parsed)
  2. SOL confirmTransaction 429时卡死→加disableRetryOnRateLimit+自动切RPC
  3. 删掉wSOL wrap（meme币不需要，浪费时间+引发429）
  4. 删掉Jito死代码（JITO_BUNDLE_API, JITO_TIP_LAMPORTS）
  5. 查余额429自动重试
  6. Provider/Connection全部单例+disableRetryOnRateLimit
  7. approve缓存key加toLowerCase()防大小写不匹配
  8. okxSign去掉body参数（GET请求不需要）

### 全链路测试结果（00:10左右）
- BSC: 买1.1s⚡ 卖5.2s ✅
- Base: 买7.2s 卖7.7s ✅（Base出块慢）
- SOL: 买0.7s⚡ 卖0.7s⚡ ✅
- gas总消耗$0.05，6/6零报错

### 速度分析
- 买入瓶颈=等链上出块确认（tx.wait(1)），无法压缩
- 可选方案：买入不等确认直接返回（快5-10倍但有revert风险）
- 跑步哥认可"代码越复杂越容易出错"，不追求极致速度

## 00:10 看板问题
- messageId 10052 editMessage可能刷太远跑步哥看不到
- 清空dashboard_state.json发新看板→messageId 10173
- 跑步哥要置顶但丢了，又发一条→messageId待确认

## 00:19 引擎自动跟单确认
- 引擎正常工作，自动买入6个币：
  - ZEC(SOL) $11 -70%、Jeffrey(SOL) $9 -72%、Doraemon(SOL) $8 -51%
  - WAKE(BSC) $5 -32%、ACE(SOL) $5 -5%、ISLAND(SOL) $5 -24%
- 跑步哥问"什么时候买的"→引擎23:20-00:11期间自动买的
- meme币波动大，大部分在跌，等SM跑了自动跟卖

## 00:21 确认门槛bug调查（结论：没bug）
- ZEC看板显示"SM×1"让跑步哥以为只有1个SM确认就买了
- 实际链路：猎手#1(CyaE1VxvBr 66.36%) + 哨兵#214 + 哨兵#229 = 1猎手+2哨兵→达标
- WAKE链路：猎手#26(0x64b23c29 61.24%) + 哨兵×3 = 1猎手+3哨兵→达标
- **根因是日志误导**：`SM确认=X`只显示猎手数，不含哨兵
- 已改成`猎手=X 哨兵=Y`，以后一目了然
- 另发现Line 890 `walletInfo?.status || 'scout'` 默认值应该是watcher不是scout（未修，暂不影响因为所有监控钱包都在wallet_db里）

## 跑步哥重申的规则
- "sm卖了你也跑啊 不要问我"→引擎自主跟卖，三链统一
- "代码越复杂越容易出错"→简单优先
- "每次sm卖了你都不跟卖，还要反过来问我"→之前是因为positions没写入的bug，现已修

## 00:30-01:07 大规模Bug Hunt（跑步哥要求"完美状态"）

### 今晚共修24个bug，三轮审计

#### 第一轮：跟单链路核心bug（8个）
1. **sellTracker纯内存→重启丢** — 改为持久化到positions._sells
2. **buyAmount=0拦跟卖** — SOL查余额返回0导致`pos.buyAmount>0`条件false，去掉该条件
3. **executeSell自己查SOL余额用429的RPC** — 改为交给dex_trader自动查
4. **所有SOL RPC用固定SOL_PUBLIC_RPC** — 全部改用getSolRpc()动态选择
5. **confirmWallets只存猎手** — 改为猎手+哨兵都存
6. **walletStatus默认值是'scout'** — 改为'watcher'
7. **-30%止损CONFIG.stopLoss定义了但没代码执行** — 补上兜底止损
8. **巡检buyPrice=0时跳过整个循环** — SM余额检查移到价格检查前面，跟卖不依赖价格

#### 第一轮其他修复
- verifyEvmSell里walletSet未定义→改用bscWalletSet/baseWalletSet
- 余额为0的持仓不清理→sell返回"余额为0"时自动delete
- notifyTelegram只打console.log→改成直接调Telegram API发通知
- 买卖通知SM×只显示猎手→改成"猎手X+哨兵Y"
- EVM地址大小写不统一→handleSignal里统一toLowerCase
- SOL轮询频率启动后不变→interval放进while循环动态调整
- 注释过期（Jito/5分钟窗口）

#### 跟卖实测验证
- 修完sellTracker+buyAmount后重启
- Jeffrey(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ACE(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ISLAND(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ZEC/Doraemon 链上余额=0→清理
- **跟卖链路从彻底断裂到完全打通**

#### 第二轮：链路衔接bug（4个）
1. **部分卖出实际全卖（Bug 22）** — executeSell不传amountRaw给dex_trader→全卖。改成查链上余额×ratio传具体数量
2. **审计失败后pendingSignals不清理** — 蜜罐币每次新信号都重复审计。审计不过直接delete
3. **买入后pendingSignals不清理** — 内存泄漏。买入前delete
4. **buyLock直接丢信号** — 两币同时确认第二个永远丢。改成等锁（最多30秒）不丢

#### 第三轮：深层衔接bug（5个）
1. **transferTracker纯内存（Bug 35）** — SM转仓到小号→重启→小号失踪。持久化到positions._transfers
2. **EVM WS转仓没写transferTracker（Bug 36）** — WS只加confirmWallets不加transferTracker，巡检漏掉
3. **全卖后sellTracker/transferTracker不清理（Bug 37）** — 内存无限膨胀
4. **SOL部分卖出parseFloat精度丢失（Bug 38）** — 大数用BigInt计算
5. **排名刷新后新钱包不加入监控（Bug 39）** — walletSet动态更新

### GEO(BSC)盈利中
- 花$5买入，现值~$47，+23%（按均价算）
- SM还没卖，继续持有
- 看板格式"$5→$46.91"是成本→现值，跑步哥问"怎么有个$5"→解释是买入成本

### 看板修复
- v8_refresh多次点击不更新：看板service内存存旧messageId(10052)，state文件写10173
- 重启v8-dashboard后正常，messageId=10173

### 当前状态（截至00:30）
- 持仓: WAKE(BSC) + GEO(BSC)，2个
- 余额: SOL~$34 BNB~$13 ETH~$100
- 引擎运行正常，巡检10秒一轮
- TG通知已打通（直接调Telegram API）

## 01:07-01:42 GEO分析 + 重大优化

### GEO持仓真相
- **实际花了$19.83买GEO，不是$5**
- 4笔买入：21:27三笔（$4.95×3，size bug导致重复买）+ 00:30一笔（$5）
- 21:27是同一个v8引擎（pid 896645），不是旧引擎
- 根因：size变量作用域bug→交易上链成功→保存positions失败→重试→重复买3次
- 00:30重启后boughtTokens丢失→又买第4次
- GEO是reflection token（持币分红），74504个→604196个自动增长8.1倍
- GEO全名"315晚会曝光毒AI"，BSC meme币

### GEO SM大户分析
- 猎手#44 0xc2c6acd3 持仓782万个≈$607 ← 大仓
- 猎手#4 0x7a2363a4 持仓308万个≈$239 ← 大仓
- 哨兵#55 0xb1e65ccc 持仓409万个≈$318 ← 大仓
- 猎手#26 0x64b23c29 只有100个≈$0.01 ← 空投
- 猎手#62 0x8d5624fa 只有100个≈$0.01 ← 空投
- SM总持仓$1,165，都没跑
- confirmWallets原来只存了两个空投钱包，大户不在跟踪列表里

### GEO收益
- 总买入$19.83，现值~$62，利润+$42，+213%，3.1x回报

### 重大优化（4项）
1. **空投/撒币过滤**: 确认达标后验证SM真实持仓，<$1的不算有效确认
   - MIN_SM_HOLDING_USD = 1
   - 防止100个token的空投触发假信号
2. **买入后新SM自动加入confirmWallets**: positions[token]存在时不买但把SM加入跟踪
   - 三条链统一
3. **看板PnL改成本回报率**: $成本→$现值 Xx格式，SM显示实际跟踪数量
4. **boughtTokens持久化**: 写入bought_tokens.json，发交易前就标记
   - 重启不丢，崩溃也不会重复买
   - 三条链统一

### GEO手动修正
- positions中busCost从$5改为$19.83
- confirmWallets从2个补到5个（加了三个大户SM）

### 教训
- **链上数据才是真相，positions记录可能不准** — 以后查持仓要交叉验证链上余额
- **reflection token会自动增长数量** — buyAmount不等于链上实际余额
- **重复买入三重防线**: txSucceeded + boughtTokens持久化 + 发交易前标记

## 01:50-02:16 第四轮审计 + GEO跟卖 + NETAINYAHU + 转账白名单

### 第四轮Bug审计（8个bug，今晚总计30个）
- **Bug 43**: dex_trader SOL余额parseInt溢出 → 改BigInt+toString
- **Bug 45**: SM卖token换USDT检测不到 → verifyEvmSell加USDT/USDC/BSC-USDC检测
- **Bug 46**: getNativePrice只用DexScreener不准($0.02) → CoinGecko优先+DexScreener fallback
- **Bug 47**: bestRank地址大小写不匹配 → 加toLowerCase对比
- **Bug 48**: bought_tokens.json文件不存在 → 从positions生成
- **Bug 49**: 巡检EVM balanceOf失败时空catch → 加日志，不再silently跳过
- DexScreener调用减半（复用空投验证数据给审计环节）
- 废弃死代码标记: parseSolanaSwap, checkLiquidity

### GEO完整交易闭环
- 链上验证发现3个大户SM(#44/#4/#55)已清仓，引擎没检测到（空catch bug）
- 手动标记_sells → 重启引擎 → 自动触发60%跟卖 ✅ tx: 0xdfa927...
- 跑步哥指出2个小仓SM(各100个)无参考意义 → 踢出confirmWallets → 重启 → 自动全卖 ✅ tx: 0x83b9be8e...
- **GEO总结**: 买入$19.83 → 两次跟卖清仓完毕
- **buyCost覆盖问题**: 手动改positions.json但引擎内存中的旧数据会在saveJSON时覆盖回去。必须改完文件立刻重启，不能让巡检间隙覆盖

### NETAINYAHU(SOL)新仓位
- 02:58:18 自动买入 $66，2猎手+1哨兵确认
- token: BBkUQdTDdVySDKXT4TngbmbpC2ktwBeymMSdn41ppump
- 引擎已自动卖出50%（2/4 SM跑了）
- SM持仓分析: 猎手#1持$1,160(大户没跑), 猎手#16持$484(标记已卖但还有大量), 哨兵#8余额=0(清仓但漏检)
- 剩余485,562个 ≈ $23.71

### 转账功能测试
- 跑步哥要求测试转账 → 成功转0.0569 SOL ≈ $5到 AVdTmb7Yrdca2foQ76jrtPQPmao6S3fziRs3zPhw4c92
- tx: 5HUR1gC2y7nPSscv45dCqqgnWq7j4wuR8u5LNJA34ooYniER27LqbBRUXhic32wSnL8iXGAtT8PMfWbxKxG1RVYq

### 转账白名单
- wallet_runtime.js加入TRANSFER_WHITELIST
- 目前白名单: AVdTmb7Yrdca2foQ76jrtPQPmao6S3fziRs3zPhw4c92 (跑步哥SOL)
- 规则: swap买卖不受限，原生币直接转账必须在白名单内
- 跑步哥尚未提供EVM白名单地址

### 关键教训
- **巡检空catch是致命bug** — SM清仓了引擎检测不到，错过跟卖时机
- **positions.json手动改会被引擎覆盖** — 改完必须立刻重启
- **空投小仓SM不应算confirmWallets** — 影响sellRatio计算

## 02:16-02:42 Bug Hunt 第五~七轮（Bug 50-76，总计53个）

### 第五轮（Bug 50-60，7个修复）
- Bug 50: SOL部分卖出RPC 429→amount=0→"Parameter amount error"，改为全卖兜底
- Bug 52: 部分卖出失败后soldRatio仍更新→下次跳过不卖，改为成功后才更新
- Bug 55: dex_trader和v8_engine双重3×3=9次重试，dex_trader去掉_withRetry
- Bug 56/59/60: 三个空catch加日志（parseSolSignature/EVM WS/managePositions）
- Bug 57: mergeToWalletDb新增钱包计数错误

### 第六轮（Bug 61-69，9个修复）
- **Bug 64: SOL WS和轮询重复检测同一信号！** 轮询遍历solWalletSet包含WS已订阅的钱包
  - 修复: `solWsSubscribedAddrs` Set，轮询时跳过WS已订阅的
- **Bug 65: parseSolSignature签名去重** `_processedSigs` Set防WS重复推送
- **Bug 66: 卖出并发锁** `_sellLocks` Set防巡检和WS同时触发executeSell
- **Bug 67: HTTP请求无超时** httpGet/httpPost加15秒timeout
- **Bug 69: DexScreener缓存** `dexScreenerGet` 10秒缓存减少API调用
- ⚠️ sed误替换dexScreenerGet→无限递归！紧急修复

### 第七轮（Bug 70-76，资金安全审计）
- **Bug 72: saveJSON文件截断风险！** writeFileSync非原子，进程crash会截断
  - 修复: write tmp → rename原子操作，wallet_db.json(452KB)安全了
- **Bug 74: 审计缓存永不过期** 蜜罐币可能被缓存为safe
  - 修复: safe结果24小时过期，unsafe永久缓存
- **Bug 75: confirmWallets无限膨胀** → 上限20个
- **Bug 76: pendingSignals内存泄漏** → 信号全过期后删key

### 检查通过的项目
- boughtTokens和positions一致性 ✅
- 止损和跟卖优先级正确 ✅
- 价格缓存安全（返回0不买入） ✅
- 主循环崩溃保护 ✅
- approve风险可接受 ✅

## 02:35-02:38 完整链路测试
- 数据加载: 4ms（319钱包/1持仓/62审计缓存）
- 排名计算: 1ms
- 钱包解密: 0ms
- 三链余额: 523ms → SOL=$382/BNB=$431/ETH=$402 → 总计$1,215
- OKX报价: SOL=111ms BSC=68ms Base=65ms 平均81ms ⚡
- 从信号到下单约500ms-1秒

## 02:40-04:00 引擎运行状态
- 检测约15个买入信号（三链），全部确认不够（单猎手为主）
- 凌晨活跃度低，正常现象
- Binancemаххіոɡ(BSC): $55买入，67%已跟卖(2/3 SM)，剩33%等最后一个SM（转仓小号）
  - 跟卖tx: 0xbedda9e86f3e81594e95467ab20c59a3aac398cd0300572b53f15a5190597d0f
- 另一个Binancemaxxx͏ing(BSC): 已全部清仓(6/6 SM跑了)
- 零ERROR，引擎稳定运行

### 转账白名单功能已删除
- 跑步哥说"感觉没必要，算了不加了"(02:16)
- wallet_runtime.js移除TRANSFER_WHITELIST等，module.exports只保留`{ getWallets }`

### 今晚Bug总计: 53个（七轮审计）

## 08:02 Heartbeat — SOL WS静默断连修复
- 发现SOL WebSocket静默断连：WS连接对象存活(无close/error事件)，但不再推送消息
- 影响：WS订阅的100个钱包（所有hunter+scout）全部漏检，过去1h只有轮询的watcher能检测
- 证据：rank #1 CyaE1V 在07:58有链上买入(pump.fun Buy)，引擎日志0记录
- 根因：Solana官方WS偶发静默断连，引擎无心跳/消息超时检测机制
- 修复：08:05重启meme-v8，WS重新建立，立即恢复检测(#34/#21/#110等)
- **TODO：需要给引擎加WS心跳超时检测——如果N分钟内WS没收到任何消息，主动重连**

## 08:09-08:32 Bug Hunt 第八~十轮 + 看板合并

### 第八轮（Bug 78-90，5个修复）
- Bug 78: SOL轮询空catch `catch(e) {}` 静默吞异常 → 加日志（过滤429噪音）
- Bug 79: EVM WS error空catch → 加日志
- Bug 80: SOL WS error空catch → 加日志
- **Bug 90: SOL WS卖出检测不区分swap/转仓！** diff<0直接标sellTracker→巡检跳过→转仓被误判为卖出
  - 修复: SOL WS改为只打日志不标sellTracker，让巡检查余额+区分转仓/卖出
  - EVM已有verifyEvmSell区分，不受影响

### 第九轮（Bug 93-99，3个修复）
- Bug 93: known_tokens.json直接writeFileSync没走原子写入 → 改为saveJSON
- **Bug 96: buyPrice=0时止损失效！** DexScreener查不到→buyPrice=0→止损条件`buyPrice>0`为false
  - 修复: 用buyCost+buyAmount+currentPrice估算PnL，止损条件改为`pnlPercent<=stopLoss && pnlPercent!==0`
- **Bug 99: 残余仓位超时！** 部分卖出后SM不跑→仓位永远挂着（止损/时间止损都不触发）
  - 修复: 持仓>24小时且soldRatio>0 → 全清

### 第十轮（Bug 100-117，2个修复+多项确认）
- **Bug 114: 没有全局错误处理！** uncaughtException/unhandledRejection→进程直接崩溃
  - 修复: process.on('uncaughtException')和process.on('unhandledRejection')，打日志不退出
- Bug 117: checkSolTransferTarget查最近3笔交易可能不包含目标token → 已知限制，影响有限

### 十轮审计总计: 66个bug

### 看板合并
- 跑步哥说"持仓跟钱包持仓重复了，合并一个好点"
- 📊持仓改为用**链上真实余额** + DexScreener价格，去掉🎒钱包持仓段
- 每个币显示链上数量（如533.5K个）
- DexScreener查价需加User-Agent否则返回1010被block

### herm持仓发现
- positions里buyAmount=0（买入时SOL RPC 429没查到），但链上实际有533,547个herm
- DexScreener价格$0.000122，实际价值≈$65，不是看板显示的$0
- 合并后用链上余额正确显示

### 看板messageId变更
- 重启看板后从旧ID→10628→10637→10642→10648
- 看板内存中messageId可能和state文件不同步，重启可恢复

### 猎手数量增长
- 84→88个猎手（排名刷新新增BSC+SOL）
- 钱包库总计336个

## 08:34-09:08 Bug Hunt 第十一~十四轮 + 交易回放 + 压测

### 第十一轮（Bug 118-124，4个修复）
- **Bug 118: buyAmount=0反复出现(herm和🏆都中招)** — SOL查余额RPC 429→catch→0
  - 修复: 加3次重试+切RPC，每次等2秒
- **Bug 121: EVM WS新钱包不实时订阅** — 排名刷新新增钱包→Set加了但WS没订阅
  - 修复: 有新EVM钱包时主动重连WS + _evmWsRefs存引用关旧连接
- **Bug 124: SOL WS静默断连根因修复** — 5分钟无消息超时检测→主动terminate+重连
- **Feature: 清仓后解锁boughtTokens** — 全卖完→从boughtTokens删除→SM再买可跟

### 第十二轮（Bug 135，1个资金安全bug）
- **Bug 135: SOL买入确认超时→重试→重复买入！** txHash存在但success=false→txSucceeded不标→重试
  - 修复: 有txHash就标txSucceeded + 确认超时查链上余额记录持仓

### 第十三轮（清理为主）
- CONFIG死配置3个标记废弃（positionSize×3, evmPollInterval, minLiqMcRatio）
- BLACKLIST变量和文件标记废弃

### 第十四轮（Bug 160，极端场景分析）
- **Bug 160: OKX API fetch无超时→buyLock永远锁住** — okxGet加15秒AbortController
- 竞态/精度/并发等9个极端场景全部分析安全

### 🚨 交易回放重大发现（Bug 166/167）
- **herm重复买入**: $72×2=$144（多花$72）— 两次独立executeBuy
- **?(GPT8cW)重复买入**: $71×2=$142（多花$71）— 同上
- 根因: handleSignal并发→两个都通过boughtTokens检查→串行executeBuy→重复
- **修复**: handleSignal通过审计后、调executeBuy前就标记boughtTokens + 买入完全失败时解锁

### 🏆(SOL)买入+止损
- 08:34:15 买入$68, 2猎手, rank#2
- 08:34:38 止损PnL=-36.3% (23秒)

### 压测结果
- 信号处理: 5000个/19.5ms = 25万/秒
- 并发买入: 10个同时→4个成功6个超时（buyLock串行）

### 当前状态(09:08)
- 持仓: Binancemаххіոɡ(BSC) 1个，$55买入，soldRatio=0.67
- 引擎运行正常，三链信号检测正常
- 今晚总计修复: 73个bug + 重复买入修复(Bug 166/167)

## 08:40-09:17 审计第12-15轮 + 安全审计 + 幽灵持仓

### 审计第12轮
- **Bug 135修复(资金安全)**: SOL买入确认超时→重试→重复买入。有txHash就标txSucceeded + 超时后查链上余额兜底
- **Bug 121补充**: EVM WS新钱包订阅→_evmWsRefs存引用，重连前terminate旧连接

### 审计第13轮（清理）
- CONFIG死配置标记废弃（positionSize×3, evmPollInterval, minLiqMcRatio）
- BLACKLIST变量/文件标记废弃
- 启动顺序、恢复链路、evict逻辑全部验证通过

### 审计第14轮（极端场景+OKX超时）
- **Bug 160(资金安全)**: OKX API fetch无超时→挂了buyLock永远锁住→加15秒AbortController
- 极端场景全分析：并发竞态、地址大小写、SM秒买秒卖、重启丢信号

### 交易回放分析（重大发现！）
- **Bug 166/167: 真实重复买入！**
  - herm: $72×2=$144（多花$72），08:10:21和08:10:25两次
  - FOOD(?): $71×2=$142（多花$71），08:50:12和08:50:17两次
  - 根因：handleSignal并发→两个都通过boughtTokens检查→buyLock串行执行→重复
  - **修复**：handleSignal通过审计后、调executeBuy前就标记boughtTokens + 失败时解锁

### 压测结果
- 信号处理：5000个/19.5ms = 25万/秒（不是瓶颈）
- 并发买入：10个同时→4个成功6个超时（buyLock串行）

### 审计第15轮（安全审计）
- **Bug 168**: Telegram Bot Token硬编码fallback→改为纯env
- .env权限644→600
- .env补齐TELEGRAM_CHAT_ID
- 看板CHAT_ID改为env优先
- 安全全面检查通过：私钥加密✅、git无泄露✅、无注入✅、端口安全✅

### FOOD幽灵持仓（跑步哥发现！）
- 09:00:28 FOOD(SOL)买入$63→交易上链成功→引擎09:01重启→positions没记录
- 链上有198万个FOOD token，价值$55
- 手动补录到positions，补了confirmWallets（猎手#2和#16）
- **教训**：审计改代码时避免在活跃交易时段频繁重启

### 今日交易汇总
| 币 | 链 | 买入 | 结果 |
|---|---|---|---|
| ACE | SOL | $5 | SM全卖跟卖 |
| ISLAND | SOL | $5 | SM全卖跟卖 |
| Jeffrey | SOL | ? | SM全卖跟卖 |
| GEO | BSC | $20 | SM跟卖+$42盈利(+213%) |
| WAKE | BSC | ? | SM跟卖50% |
| Agent | SOL | $6 | SM全卖跟卖 |
| NETAINYAHU | SOL | $66 | SM全卖跟卖 |
| Binancemаххіոɡ | BSC | $55 | 67%已卖，剩余持仓中 |
| Binancemaxxx͏ing | BSC | $67 | SM全卖跟卖 |
| herm | SOL | $144(重复!) | SM全卖跟卖 |
| 🏆 | SOL | $68 | 23秒止损-36% |
| FOOD | SOL | $142(重复!) | 持仓中-12.6% |

### 15轮审计总计: 74个bug
- 资金安全级: Bug 135(重复买入-重试), Bug 160(OKX超时锁死), Bug 166/167(重复买入-并发)
- 重复买入实际损失: $143
- boughtTokens清仓解锁已实现

### 当前状态(09:17)
- 持仓2个: Binancemаххіոɡ(BSC) + FOOD(SOL)
- 引擎运行正常
- 余额: SOL≈$91 / BNB≈$671 / ETH≈$2170
