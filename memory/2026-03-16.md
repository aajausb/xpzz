# 2026-03-16 日记

## 00:00-00:30 dex_trader v2 重写 + 全链路测试

### dex_trader.js 完整重写（v2）
- 从430行精简到280行，修掉8个问题：
  1. SOL sell查余额用`getTokenAccountsByOwner`(raw buffer)→改`getParsedTokenAccountsByOwner`(parsed)
  2. SOL confirmTransaction 429时卡死→加disableRetryOnRateLimit+自动切RPC
  3. 删掉wSOL wrap（meme币不需要，浪费时间+引发429）
  4. 删掉Jito死代码（JITO_BUNDLE_API, JITO_TIP_LAMPORTS）
  5. 查余额429自动重试
  6. Provider/Connection全部单例+disableRetryOnRateLimit
  7. approve缓存key加toLowerCase()防大小写不匹配
  8. okxSign去掉body参数（GET请求不需要）

### 全链路测试结果（00:10左右）
- BSC: 买1.1s⚡ 卖5.2s ✅
- Base: 买7.2s 卖7.7s ✅（Base出块慢）
- SOL: 买0.7s⚡ 卖0.7s⚡ ✅
- gas总消耗$0.05，6/6零报错

### 速度分析
- 买入瓶颈=等链上出块确认（tx.wait(1)），无法压缩
- 可选方案：买入不等确认直接返回（快5-10倍但有revert风险）
- 跑步哥认可"代码越复杂越容易出错"，不追求极致速度

## 00:10 看板问题
- messageId 10052 editMessage可能刷太远跑步哥看不到
- 清空dashboard_state.json发新看板→messageId 10173
- 跑步哥要置顶但丢了，又发一条→messageId待确认

## 00:19 引擎自动跟单确认
- 引擎正常工作，自动买入6个币：
  - ZEC(SOL) $11 -70%、Jeffrey(SOL) $9 -72%、Doraemon(SOL) $8 -51%
  - WAKE(BSC) $5 -32%、ACE(SOL) $5 -5%、ISLAND(SOL) $5 -24%
- 跑步哥问"什么时候买的"→引擎23:20-00:11期间自动买的
- meme币波动大，大部分在跌，等SM跑了自动跟卖

## 00:21 确认门槛bug调查（结论：没bug）
- ZEC看板显示"SM×1"让跑步哥以为只有1个SM确认就买了
- 实际链路：猎手#1(CyaE1VxvBr 66.36%) + 哨兵#214 + 哨兵#229 = 1猎手+2哨兵→达标
- WAKE链路：猎手#26(0x64b23c29 61.24%) + 哨兵×3 = 1猎手+3哨兵→达标
- **根因是日志误导**：`SM确认=X`只显示猎手数，不含哨兵
- 已改成`猎手=X 哨兵=Y`，以后一目了然
- 另发现Line 890 `walletInfo?.status || 'scout'` 默认值应该是watcher不是scout（未修，暂不影响因为所有监控钱包都在wallet_db里）

## 跑步哥重申的规则
- "sm卖了你也跑啊 不要问我"→引擎自主跟卖，三链统一
- "代码越复杂越容易出错"→简单优先
- "每次sm卖了你都不跟卖，还要反过来问我"→之前是因为positions没写入的bug，现已修

## 00:30-01:07 大规模Bug Hunt（跑步哥要求"完美状态"）

### 今晚共修24个bug，三轮审计

#### 第一轮：跟单链路核心bug（8个）
1. **sellTracker纯内存→重启丢** — 改为持久化到positions._sells
2. **buyAmount=0拦跟卖** — SOL查余额返回0导致`pos.buyAmount>0`条件false，去掉该条件
3. **executeSell自己查SOL余额用429的RPC** — 改为交给dex_trader自动查
4. **所有SOL RPC用固定SOL_PUBLIC_RPC** — 全部改用getSolRpc()动态选择
5. **confirmWallets只存猎手** — 改为猎手+哨兵都存
6. **walletStatus默认值是'scout'** — 改为'watcher'
7. **-30%止损CONFIG.stopLoss定义了但没代码执行** — 补上兜底止损
8. **巡检buyPrice=0时跳过整个循环** — SM余额检查移到价格检查前面，跟卖不依赖价格

#### 第一轮其他修复
- verifyEvmSell里walletSet未定义→改用bscWalletSet/baseWalletSet
- 余额为0的持仓不清理→sell返回"余额为0"时自动delete
- notifyTelegram只打console.log→改成直接调Telegram API发通知
- 买卖通知SM×只显示猎手→改成"猎手X+哨兵Y"
- EVM地址大小写不统一→handleSignal里统一toLowerCase
- SOL轮询频率启动后不变→interval放进while循环动态调整
- 注释过期（Jito/5分钟窗口）

#### 跟卖实测验证
- 修完sellTracker+buyAmount后重启
- Jeffrey(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ACE(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ISLAND(SOL) SM全部卖出(2/2)→✅ 自动跟卖成功
- ZEC/Doraemon 链上余额=0→清理
- **跟卖链路从彻底断裂到完全打通**

#### 第二轮：链路衔接bug（4个）
1. **部分卖出实际全卖（Bug 22）** — executeSell不传amountRaw给dex_trader→全卖。改成查链上余额×ratio传具体数量
2. **审计失败后pendingSignals不清理** — 蜜罐币每次新信号都重复审计。审计不过直接delete
3. **买入后pendingSignals不清理** — 内存泄漏。买入前delete
4. **buyLock直接丢信号** — 两币同时确认第二个永远丢。改成等锁（最多30秒）不丢

#### 第三轮：深层衔接bug（5个）
1. **transferTracker纯内存（Bug 35）** — SM转仓到小号→重启→小号失踪。持久化到positions._transfers
2. **EVM WS转仓没写transferTracker（Bug 36）** — WS只加confirmWallets不加transferTracker，巡检漏掉
3. **全卖后sellTracker/transferTracker不清理（Bug 37）** — 内存无限膨胀
4. **SOL部分卖出parseFloat精度丢失（Bug 38）** — 大数用BigInt计算
5. **排名刷新后新钱包不加入监控（Bug 39）** — walletSet动态更新

### GEO(BSC)盈利中
- 花$5买入，现值~$47，+23%（按均价算）
- SM还没卖，继续持有
- 看板格式"$5→$46.91"是成本→现值，跑步哥问"怎么有个$5"→解释是买入成本

### 看板修复
- v8_refresh多次点击不更新：看板service内存存旧messageId(10052)，state文件写10173
- 重启v8-dashboard后正常，messageId=10173

### 当前状态
- 持仓: WAKE(BSC) + GEO(BSC)，2个
- 余额: SOL~$34 BNB~$13 ETH~$100
- 引擎运行正常，巡检10秒一轮
- TG通知已打通（直接调Telegram API）
