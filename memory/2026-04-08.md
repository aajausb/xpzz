# 2026-04-08

## 凌晨 Gift BSC调查
- 跑步哥问Gift BSC (0x9f465f7ad7bb42665e22d55bad322f4726a6ffff) 的叙事
- 搜遍了Google/DexScreener/Reddit/小红书都搜不到有用信息

## 早上 signal_analyzer.js prompt大改 + 多项修复 (08:07-08:59)

### AI Prompt改版（四维度）
- 旧prompt: 5维度（叙事/SM态度/盘面/社区/推特热度），评判标准偏Web2（看有没有网站推特）
- 新prompt: 4维度，Web3思维：
  1. SM共识(30分) — SM数量×持仓占市值比×猎手vs哨兵
  2. 盘面动能(25分) — 换手率+买卖比+流动性占市值比
  3. 叙事传播力(25分) — 一句话说清+情绪触发+催化剂（不看有没有网站）
  4. 风险信号(20分) — LP burn+流动性+持仓集中度
- 跑步哥要求去掉"GitHub垃圾币"检查项
- 格式：SM/盘面/风险各一句话，叙事2-3句，总分+emoji+一句话总结
- prompt预计算辅助数据：SM占市值%、换手率%、买盘占比%

### HTML转义bug修复
- AI分析输出含`<`/`>`会被TG当HTML标签解析失败
- 在buildMessage里加了`&amp;`/`&lt;`/`&gt;`转义

### 买入按钮默认生成
- 手动分析也带买入按钮（买$100/买$200/买$300/跳过）
- sendTG函数：没传buttons但有token时自动生成

### TG发送重试机制
- 发现8小时内TG发送失败7次（33%失败率），WOOZY/BULL/INU/AGNT等通知丢失
- 原因：国内连TG API不稳定，超时后resolve({})不阻断流程，日志假"成功"
- 修复：加sendTGWithRetry，失败后等3秒重试一次

### 手动分析流程
- 跑步哥发合约→我查DexScreener+pending_signals→喂analyzer→发TG
- pending_signals有SM数据就用，没有就填0并告知
- **教训：查不到SM数据不能默默填0，必须告诉跑步哥**（49事件：pending里0但实际2H+3S）
- DuckDuckGo连续搜多次会限流（验证码），引擎自动触发间隔够长一般没事

### Fusecode账户
- 套餐：Pro Max Plan Claude，余额$998.46，到期2026-04-15
- 周限额$1200/周，analyzer每次分析消耗约$0.2-0.5，不用担心

## 上午 自动买入+SM链上查询+通知格式优化 (09:00-11:53)

### 自动买入实现
- 评分≥70自动买入$200，三链统一
- 评分<70只发通知+按钮
- 买入最多重试3次，间隔3秒
- 成功后自动写positions.json，引擎巡检止盈止损
- TG通知标注"🤖 自动买入$200 ✅"或"❌ 失败"

### SM链上查询集成到signal_analyzer.js
- smLiveTotal=0时自动从pending_signals找SM钱包地址→查链上余额→算总值
- SOL: getTokenAccountsByOwner (SPL Token Program)
- EVM: OKX钱包API (已修decimals除法)
- 测试：Optimistic 3个钱包查到2个持仓$2493

### 通知格式优化
- 有SM持仓：`📊 SM 2/3个仍持仓 | 实时持有$2493`
- SM全跑了：`📊 SM 3个曾买入 | ⚠️ 全部已清仓`
- 风险维度去掉"持仓集中度"（没数据就不评）

### 手动买入
- Optimistic $100 (09:04) — 手动调dex_trader买的，需手动加positions
- stonks $200 (11:38) — 跑步哥点按钮+确认买入
- 当前3个持仓：BURNIE $300 / Optimistic $100 / stonks $200

### 链上确认
- 49的SM全跑了（109个SOL钱包遍历，0个持有）
- Iroha的SM全跑了（3个pending + 109个遍历，0个持有）
- SOL充值后余额12.54 SOL

### 规则确认
- 市值门槛：统一$10K（CONFIG.minMarketCap: 10000），MEMORY.md已更新
- 手动分析必须查SM链上余额，不能填0
- 以后发合约直接分析发结果，不多废话
- 按钮callback handler还没实现

## 中午 自动买入+buy_queue修复 (12:00-12:30)

### 自动买入实战
- 第一次实战：49(solana) 评分75≥70触发自动买入
- **失败原因**：CoinGecko限流→DexScreener fallback拿SOL价格可能不准→lamports计算异常→OKX报"价格差异>90%"拒绝报价
- 3次重试全失败，都是同一个原因
- 手动重试成功（直接传正确lamports），49买入$200 TX: 46xJan3chA4ZTRiuNADT...
- **待修**：getNativePrice价格合理性检查

### buy_queue机制
- **问题发现**：analyzer写positions.json会被引擎内存覆盖（stonks手动买入后消失）
- **方案**：analyzer写buy_queue.json → 引擎巡检开头读queue → 加入内存positions
- analyzer改完（不写positions，改写buy_queue.json）
- 引擎`managePositions()`开头加读buy_queue逻辑
- **实战验证成功**：49写入buy_queue → 引擎5秒内读取 → "🤖 从buy_queue加入持仓: 49(solana) $200"
- 以后手动调dex_trader买入也要写buy_queue

### getNativePrice双fallback
- CoinGecko优先，失败用DexScreener查wrapped token（WSOL/WBNB/WETH）
- CoinGecko免费API限流严重，DexScreener更稳定
- 但DexScreener查wrapped token的价格逻辑可能有坑（base/quote方向），这次49自动买入失败就是价格不准

### 引擎重启
- 12:22重启：加stonks到positions + buy_queue机制生效
- 当前4个持仓：BURNIE $300 / Optimistic $100 / stonks $200 / 49 $200

### 信号记录
- VDOR(solana) 猎手=2 09:57 — 通知模式
- ROAR(solana) 猎手=2 10:02 — 通知模式
- WRT(solana) 猎手=2 10:39 — 通知模式
- stonks(solana) 1猎+3哨 11:33 — 通知模式
- 49(solana) 评分75 12:23 — 自动买入失败→手动补买成功
- 叙事是"Four平台bug发了貔貅币被CTO"——中文加密社区内部传播的八卦，只在推特/TG群讨论
- **教训**: 社区叙事和八卦靠链上数据和网站抓取搜不到，必须配xreach推特搜索
- xreach推特cookie仍未配置（跑步哥说会从电脑上操作）

## 当前搜索能力缺口
- 推特: xreach没配cookie → 搜不到中文加密推特
- 小红书: mcporter离线
- Google: 被反爬，Jina Reader对搜索页效果差
- **结论**: 中文加密社区热点叙事是当前最大信息盲区
